import javax.spring.*;
import javax.swing.event.*;
import.javax.awt.BorderLayout;

publi class mainMenu
{
   
   public void mainMenu
/*
	private JSpinner timeOfDay;
	private JLabel label = new Label("", JLabel.LEFT);}
	public mainMenu()
	{
		getContentPane().add(spinner, BorderLayout.NORTH);
		getContentPane().add(spinner, BorderLayout.SOUTH);
		
		spinner.addChangeListener(new ChangeListener()
		{
			public void stateChanged(javax,swing.event.ChangeEvent e)
			{
				label.setText ("Previous value: " + spinner.getPreviousValue()
			}
		}
	*/
	}
